enum RouteNames {
  Login = '/login',
  Detail = '/:type/detail',
  Register = '/register',
  Users = '/users',
  Posts = '/posts',
  Products = '/products',
  GithubSearch = '/github-search',
}

export default RouteNames;
